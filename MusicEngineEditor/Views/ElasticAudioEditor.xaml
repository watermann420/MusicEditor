<UserControl x:Class="MusicEngineEditor.Views.ElasticAudioEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:MusicEngineEditor.Controls"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Toolbar Button Style -->
        <Style x:Key="WarpToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource DisabledForegroundBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style for BPM inputs -->
        <Style x:Key="BpmTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="#383838"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Button Style for Warp Enable -->
        <Style x:Key="WarpToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Header -->
                <RowDefinition Height="*"/>      <!-- Warp Editor -->
                <RowDefinition Height="Auto"/>  <!-- Status Bar -->
            </Grid.RowDefinitions>

            <!-- Header with clip info and controls -->
            <Border Grid.Row="0"
                    Background="{DynamicResource ToolbarBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: Title and Clip Info -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Elastic Audio Editor"
                                   FontWeight="Bold"
                                   FontSize="14"
                                   Foreground="{DynamicResource BrightForegroundBrush}"
                                   VerticalAlignment="Center"/>

                        <TextBlock x:Name="ClipNameText"
                                   Text=" - No clip loaded"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   FontSize="13"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"/>

                        <Rectangle Width="1"
                                   Fill="{DynamicResource SubtleBorderBrush}"
                                   Margin="16,2"/>

                        <!-- Warp Enable Toggle -->
                        <ToggleButton x:Name="WarpEnableToggle"
                                      Content="Warp"
                                      Style="{StaticResource WarpToggleStyle}"
                                      IsChecked="True"
                                      Click="WarpEnableToggle_Click"
                                      ToolTip="Enable/disable warping for this clip"/>
                    </StackPanel>

                    <!-- Right: BPM Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Original BPM -->
                        <TextBlock Text="Original BPM:"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>

                        <TextBox x:Name="OriginalBpmBox"
                                 Width="65"
                                 Text="120.0"
                                 Style="{StaticResource BpmTextBoxStyle}"
                                 TextAlignment="Center"
                                 LostFocus="OriginalBpmBox_LostFocus"
                                 KeyDown="BpmBox_KeyDown"/>

                        <Button x:Name="DetectBpmBtn"
                                Content="Detect"
                                Style="{StaticResource WarpToolbarButtonStyle}"
                                Click="DetectBpm_Click"
                                ToolTip="Auto-detect original BPM from audio"
                                Margin="6,0,0,0"/>

                        <Rectangle Width="1"
                                   Fill="{DynamicResource SubtleBorderBrush}"
                                   Margin="16,2"/>

                        <!-- Project BPM -->
                        <TextBlock Text="Project BPM:"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>

                        <TextBox x:Name="ProjectBpmBox"
                                 Width="65"
                                 Text="120.0"
                                 Style="{StaticResource BpmTextBoxStyle}"
                                 TextAlignment="Center"
                                 LostFocus="ProjectBpmBox_LostFocus"
                                 KeyDown="BpmBox_KeyDown"/>

                        <Button x:Name="MatchBpmBtn"
                                Content="Match"
                                Style="{StaticResource WarpToolbarButtonStyle}"
                                Click="MatchBpm_Click"
                                ToolTip="Match clip to project tempo by adjusting warp markers"
                                Margin="6,0,0,0"/>

                        <Rectangle Width="1"
                                   Fill="{DynamicResource SubtleBorderBrush}"
                                   Margin="16,2"/>

                        <!-- Algorithm Selection -->
                        <TextBlock Text="Algorithm:"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>

                        <ComboBox x:Name="AlgorithmCombo"
                                  Width="100"
                                  SelectedIndex="2"
                                  SelectionChanged="AlgorithmCombo_SelectionChanged"
                                  ToolTip="Time stretch algorithm">
                            <ComboBoxItem Content="Tonal"/>
                            <ComboBoxItem Content="Transient"/>
                            <ComboBoxItem Content="Complex"/>
                            <ComboBoxItem Content="Repitch"/>
                            <ComboBoxItem Content="Texture"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Warp Marker Control -->
            <controls:WarpMarkerEditor x:Name="WarpMarkerEditor"
                                        Grid.Row="1"
                                        Margin="0,1,0,0"
                                        MarkerAdded="WarpMarkerEditor_MarkerAdded"
                                        MarkerDeleted="WarpMarkerEditor_MarkerDeleted"
                                        MarkerMoved="WarpMarkerEditor_MarkerMoved"
                                        AutoDetectRequested="WarpMarkerEditor_AutoDetectRequested"/>

            <!-- Status Bar -->
            <Border Grid.Row="2"
                    Background="{DynamicResource AccentBrush}"
                    Padding="8,4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Status Message -->
                    <TextBlock x:Name="StatusText"
                               Grid.Column="0"
                               Text="Ready - Load a clip to begin editing"
                               Foreground="White"
                               FontSize="11"
                               VerticalAlignment="Center"/>

                    <!-- Marker Count -->
                    <TextBlock Grid.Column="1"
                               Foreground="White"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Margin="16,0">
                        <Run Text="Markers: "/>
                        <Run x:Name="MarkerCountRun" Text="2"/>
                    </TextBlock>

                    <!-- Stretch Range -->
                    <TextBlock Grid.Column="2"
                               Foreground="White"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Margin="0,0,16,0">
                        <Run Text="Stretch Range: "/>
                        <Run x:Name="StretchRangeRun" Text="1.00x"/>
                    </TextBlock>

                    <!-- Duration Change -->
                    <TextBlock Grid.Column="3"
                               Foreground="White"
                               FontSize="11"
                               VerticalAlignment="Center">
                        <Run Text="Duration: "/>
                        <Run x:Name="DurationChangeRun" Text="100%"/>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
