<Window x:Class="MusicEngineEditor.Views.Dialogs.ShortcutsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MusicEngineEditor.Controls"
        xmlns:vm="clr-namespace:MusicEngineEditor.ViewModels"
        xmlns:models="clr-namespace:MusicEngineEditor.Models"
        xmlns:local="clr-namespace:MusicEngineEditor.Views.Dialogs"
        mc:Ignorable="d"
        Title="Keyboard Shortcuts"
        Width="800" Height="600"
        MinWidth="700" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}"
        d:DataContext="{d:DesignInstance Type=vm:ShortcutsViewModel}">

    <Window.Resources>
        <!-- Null to visibility converter -->
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

        <!-- Category header style -->
        <Style x:Key="CategoryExpanderStyle" TargetType="Expander">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Foreground" Value="{DynamicResource BrightForegroundBrush}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <DockPanel>
                                <ToggleButton x:Name="HeaderToggle"
                                              DockPanel.Dock="Top"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand"
                                              Padding="{TemplateBinding Padding}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock x:Name="Arrow"
                                                               Text="&#9654;"
                                                               FontSize="10"
                                                               VerticalAlignment="Center"
                                                               Foreground="{DynamicResource SecondaryForegroundBrush}"/>

                                                    <ContentPresenter Grid.Column="1"
                                                                      Content="{TemplateBinding Content}"
                                                                      Margin="8,0,0,0"
                                                                      VerticalAlignment="Center"/>

                                                    <TextBlock Grid.Column="2"
                                                               Text="{Binding Shortcuts.Count, StringFormat='({0})'}"
                                                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                               FontSize="11"
                                                               VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="Arrow" Property="Text" Value="&#9660;"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                    <TextBlock Text="{Binding DisplayName}"
                                               FontWeight="SemiBold"
                                               FontSize="13"/>
                                </ToggleButton>

                                <ContentPresenter x:Name="ContentArea"
                                                  DockPanel.Dock="Bottom"
                                                  Visibility="Collapsed"/>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ContentArea" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Shortcut item style -->
        <DataTemplate x:Key="ShortcutItemTemplate" DataType="{x:Type vm:ShortcutItemViewModel}">
            <Border x:Name="ItemBorder"
                    Background="Transparent"
                    Padding="16,10,12,10"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Command info -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Description}"
                                   Foreground="{DynamicResource ForegroundBrush}"
                                   FontSize="13"/>
                        <TextBlock Text="{Binding CommandName}"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   FontSize="11"
                                   Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Shortcut display -->
                    <Border Grid.Column="1"
                            Background="{DynamicResource EditorBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="10,6"
                            Margin="12,0"
                            HorizontalAlignment="Stretch">
                        <TextBlock Text="{Binding DisplayString}"
                                   Foreground="{DynamicResource ForegroundBrush}"
                                   FontFamily="Consolas, Courier New"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <!-- Modified indicator -->
                    <Ellipse x:Name="ModifiedIndicator"
                             Grid.Column="2"
                             Width="8" Height="8"
                             Fill="{DynamicResource AccentBrush}"
                             VerticalAlignment="Center"
                             Visibility="Collapsed"
                             ToolTip="Modified from default"/>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SelectionBackgroundBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsModified}" Value="True">
                    <Setter TargetName="ModifiedIndicator" Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsCustomizable}" Value="False">
                    <Setter TargetName="ItemBorder" Property="Opacity" Value="0.6"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                </Trigger>
            </DataTemplate.Triggers>
        </DataTemplate>

        <!-- Category to string converter -->
        <Style x:Key="CategoryComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="8,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Search Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search box -->
                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Background="{DynamicResource EditorBackgroundBrush}"
                         Foreground="{DynamicResource ForegroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         Padding="10,8"
                         FontSize="13"
                         VerticalContentAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="&#x1F50D;"
                                                           FontSize="14"
                                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                           VerticalAlignment="Center"
                                                           Margin="10,0,8,0"/>
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                              Grid.Column="1"
                                                              Padding="{TemplateBinding Padding}"
                                                              VerticalAlignment="Center"/>
                                                <TextBlock x:Name="Placeholder"
                                                           Grid.Column="1"
                                                           Text="Search shortcuts..."
                                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                                           VerticalAlignment="Center"
                                                           Margin="10,0,0,0"
                                                           IsHitTestVisible="False"
                                                           Visibility="Collapsed"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                            <Trigger Property="Text" Value="{x:Null}">
                                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Category filter -->
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding AvailableCategories}"
                          SelectedItem="{Binding SelectedCategory}"
                          Width="150"
                          Margin="12,0,0,0"
                          Background="{DynamicResource EditorBackgroundBrush}"
                          Foreground="{DynamicResource ForegroundBrush}"
                          BorderBrush="{DynamicResource BorderBrush}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                                <Setter Property="Text" Value="All Categories"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Expand/Collapse buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="12,0,0,0">
                    <Button Content="Expand All"
                            Command="{Binding ExpandAllCommand}"
                            Padding="10,6"
                            Margin="0,0,6,0"/>
                    <Button Content="Collapse All"
                            Command="{Binding CollapseAllCommand}"
                            Padding="10,6"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Shortcuts list -->
            <ScrollViewer Grid.Column="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding FilteredCategories}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ShortcutCategoryViewModel}">
                            <Expander Style="{StaticResource CategoryExpanderStyle}"
                                      IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
                                <ItemsControl ItemsSource="{Binding Shortcuts}"
                                              ItemTemplate="{StaticResource ShortcutItemTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Edit panel -->
            <Border Grid.Column="1"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Edit Shortcut"
                                   FontWeight="SemiBold"
                                   FontSize="14"
                                   Margin="0,0,0,16"
                                   Foreground="{DynamicResource BrightForegroundBrush}"/>

                        <!-- Selected shortcut info -->
                        <StackPanel Visibility="{Binding SelectedShortcut, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert, FallbackValue=Collapsed}">
                            <TextBlock Text="{Binding SelectedShortcut.Description}"
                                       FontSize="13"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,4"/>

                            <TextBlock Text="{Binding SelectedShortcut.CommandName}"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                                       Margin="0,0,0,16"/>

                            <!-- Shortcut recorder -->
                            <TextBlock Text="Shortcut"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                                       Margin="0,0,0,6"/>

                            <controls:ShortcutRecorder x:Name="ShortcutRecorderControl"
                                                       CurrentKey="{Binding SelectedShortcut.Key, Mode=TwoWay}"
                                                       CurrentModifiers="{Binding SelectedShortcut.Modifiers, Mode=TwoWay}"
                                                       IsCustomizable="{Binding SelectedShortcut.IsCustomizable}"
                                                       ShortcutRecorded="ShortcutRecorder_ShortcutRecorded"
                                                       ShortcutCleared="ShortcutRecorder_ShortcutCleared"
                                                       Margin="0,0,0,12"/>

                            <!-- Conflict message -->
                            <Border Background="#3D2B2B"
                                    BorderBrush="#E06C75"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="10"
                                    Margin="0,0,0,12"
                                    Visibility="{Binding ConflictMessage, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert, FallbackValue=Collapsed}">
                                <TextBlock Text="{Binding ConflictMessage}"
                                           Foreground="#E06C75"
                                           FontSize="11"
                                           TextWrapping="Wrap"/>
                            </Border>

                            <!-- Action buttons -->
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <Button Content="Clear"
                                        Command="{Binding ClearShortcutCommand}"
                                        Padding="12,6"
                                        Margin="0,0,8,0"
                                        IsEnabled="{Binding SelectedShortcut.IsCustomizable}"/>
                                <Button Content="Reset"
                                        Command="{Binding ResetShortcutCommand}"
                                        Padding="12,6"
                                        ToolTip="Reset to default"/>
                            </StackPanel>

                            <Separator Background="{DynamicResource BorderBrush}" Margin="0,20"/>

                            <!-- Category info -->
                            <TextBlock Text="Category"
                                       FontSize="11"
                                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding SelectedShortcut.Category}"
                                       FontSize="13"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,12"/>

                            <!-- Customizable info -->
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="Customizable:"
                                           FontSize="11"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                           Margin="0,0,6,0"/>
                                <TextBlock FontSize="11">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Yes"/>
                                            <Setter Property="Foreground" Value="#00FF88"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedShortcut.IsCustomizable}" Value="False">
                                                    <Setter Property="Text" Value="No"/>
                                                    <Setter Property="Foreground" Value="#E06C75"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <!-- No selection message -->
                        <TextBlock Text="Select a shortcut to edit"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Margin="0,40,0,0"
                                   Visibility="{Binding SelectedShortcut, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Visible}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Import/Export bar -->
        <Border Grid.Row="2"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Export..."
                        Command="{Binding ExportShortcutsCommand}"
                        Padding="12,6"
                        Margin="0,0,8,0"/>
                <Button Content="Import..."
                        Command="{Binding ImportShortcutsCommand}"
                        Padding="12,6"
                        Margin="0,0,8,0"/>
                <Button Content="Reset All to Defaults"
                        Command="{Binding ResetAllToDefaultsCommand}"
                        Padding="12,6"/>
            </StackPanel>
        </Border>

        <!-- Bottom button panel -->
        <Border Grid.Row="3"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,12">
            <Grid>
                <!-- Status message -->
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"/>

                <!-- OK/Cancel buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="OK"
                            IsDefault="True"
                            Command="{Binding ApplyCommand}"
                            Padding="20,8"
                            Margin="0,0,10,0"
                            Background="{DynamicResource AccentBrush}"
                            Foreground="White"/>
                    <Button Content="Cancel"
                            IsCancel="True"
                            Command="{Binding CancelCommand}"
                            Padding="20,8"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
