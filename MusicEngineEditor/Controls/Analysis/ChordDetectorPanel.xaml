<UserControl x:Class="MusicEngineEditor.Controls.Analysis.ChordDetectorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Controls.Analysis"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="400"
             Background="{DynamicResource BackgroundBrush}"
             MinHeight="400"
             MinWidth="320">

    <UserControl.Resources>
        <!-- Local aliases referencing global theme resources -->
        <SolidColorBrush x:Key="PanelBrush" Color="{DynamicResource PanelBackgroundColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{DynamicResource ForegroundColor}"/>
        <SolidColorBrush x:Key="DimTextBrush" Color="{DynamicResource SecondaryForegroundColor}"/>
        <SolidColorBrush x:Key="WhiteKeyBrush" Color="{DynamicResource ForegroundColor}"/>
        <SolidColorBrush x:Key="BlackKeyBrush" Color="{DynamicResource BorderColor}"/>
        <SolidColorBrush x:Key="HighlightedKeyBrush" Color="{DynamicResource AccentColor}"/>

        <!-- Confidence Gradient -->
        <LinearGradientBrush x:Key="ConfidenceGradientBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="{DynamicResource ErrorColor}" Offset="0.0"/>
            <GradientStop Color="{DynamicResource WarningColor}" Offset="0.5"/>
            <GradientStop Color="{DynamicResource SuccessColor}" Offset="0.8"/>
            <GradientStop Color="{DynamicResource AccentColor}" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- Button Style -->
        <Style x:Key="ChordDetectorButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect x:Name="glowEffect" Color="{DynamicResource AccentColor}"
                                                  BlurRadius="0" ShadowDepth="0" Opacity="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="BlurRadius"
                                                             To="8" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.6" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="BlurRadius"
                                                             To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SubtleBorderBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource BackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Button Style -->
        <Style x:Key="ChordDetectorToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DimTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect x:Name="glowEffect" Color="{DynamicResource AccentColor}"
                                                  BlurRadius="0" ShadowDepth="0" Opacity="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource BackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="BlurRadius"
                                                             To="6" Duration="0:0:0.12"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.5" Duration="0:0:0.12"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="BlurRadius"
                                                             To="0" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="glowEffect"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource SubtleBorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Slider Style -->
        <Style x:Key="ChordDetectorSliderStyle" TargetType="Slider">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Height" Value="20"/>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ChordDetectorComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <!-- Section Label Style -->
        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource DimTextBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- ListBox Style -->
        <Style x:Key="ChordDetectorListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>
    </UserControl.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Background="{DynamicResource BackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="6,6,0,0"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Chord Detector"
                               FontWeight="SemiBold"
                               FontSize="14"
                               Foreground="{StaticResource TextBrush}"
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <ToggleButton x:Name="DetectionToggle"
                                      Style="{StaticResource ChordDetectorToggleStyle}"
                                      Content="Active"
                                      IsChecked="{Binding IsDetectionActive, Mode=TwoWay}"
                                      ToolTip="Enable/disable chord detection"
                                      Margin="0,0,8,0"/>
                        <Button Style="{StaticResource ChordDetectorButtonStyle}"
                                Content="Reset"
                                Command="{Binding ResetDetectionCommand}"
                                ToolTip="Reset detection"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Chord Display -->
            <Border Grid.Row="1"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="16,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Chord Name and Quality -->
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="ChordNameText"
                                   Text="{Binding ChordName}"
                                   FontSize="48"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="{Binding QualityText}"
                                       FontSize="14"
                                       Foreground="{StaticResource TextBrush}"/>
                            <TextBlock x:Name="SlashChordIndicator"
                                       Text=" (Slash Chord)"
                                       FontSize="12"
                                       Foreground="{StaticResource DimTextBrush}"
                                       Visibility="{Binding IsSlashChord, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0"
                                    Visibility="{Binding IsSlashChord, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="Bass: "
                                       FontSize="12"
                                       Foreground="{StaticResource DimTextBrush}"/>
                            <TextBlock Text="{Binding BassNote}"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource WarningBrush}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Roman Numeral -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Roman Numeral"
                                   FontSize="10"
                                   Foreground="{StaticResource DimTextBrush}"
                                   HorizontalAlignment="Center"/>
                        <Border Background="{DynamicResource PanelBackgroundBrush}"
                                CornerRadius="6"
                                Padding="16,8"
                                Margin="0,4,0,0">
                            <TextBlock x:Name="RomanNumeralText"
                                       Text="{Binding RomanNumeral}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="{StaticResource SuccessBrush}"
                                       HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Confidence Meter -->
            <Border Grid.Row="2"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Detection Confidence"
                                   FontSize="11"
                                   Foreground="{StaticResource DimTextBrush}"
                                   Margin="0,0,0,4"/>
                        <Grid>
                            <Border Background="{DynamicResource PanelBackgroundBrush}"
                                    CornerRadius="4"
                                    Height="10">
                                <Border x:Name="ConfidenceFill"
                                        Background="{StaticResource ConfidenceGradientBrush}"
                                        CornerRadius="4"
                                        HorizontalAlignment="Left"
                                        Width="0"/>
                            </Border>
                        </Grid>
                    </StackPanel>

                    <TextBlock Grid.Column="1"
                               x:Name="ConfidenceText"
                               Text="{Binding ConfidenceText}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               VerticalAlignment="Center"
                               Margin="16,0,0,0"/>
                </Grid>
            </Border>

            <!-- Piano Keyboard Visualization -->
            <Border Grid.Row="3"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,10">
                <StackPanel>
                    <TextBlock Text="DETECTED NOTES"
                               Style="{StaticResource SectionLabelStyle}"/>
                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                            CornerRadius="4"
                            Height="60"
                            Margin="0,4,0,0">
                        <Canvas x:Name="PianoKeyboardCanvas"
                                ClipToBounds="True"
                                SizeChanged="PianoKeyboardCanvas_SizeChanged"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Guitar Chord Diagram -->
            <Border Grid.Row="4"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,10"
                    Visibility="{Binding HasGuitarDiagram, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="GUITAR FINGERING"
                                   Style="{StaticResource SectionLabelStyle}"/>
                        <Border Background="{DynamicResource PanelBackgroundBrush}"
                                CornerRadius="4"
                                Width="100"
                                Height="120"
                                Margin="0,4,0,0">
                            <Canvas x:Name="GuitarDiagramCanvas"
                                    ClipToBounds="True"
                                    SizeChanged="GuitarDiagramCanvas_SizeChanged"/>
                        </Border>
                    </StackPanel>

                    <!-- Alternative Chords -->
                    <StackPanel Grid.Column="1" Margin="16,0,0,0">
                        <TextBlock Text="ALTERNATIVE INTERPRETATIONS"
                                   Style="{StaticResource SectionLabelStyle}"/>
                        <ItemsControl ItemsSource="{Binding AlternativeChords}"
                                      MaxHeight="120">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="3"
                                            Padding="8,4"
                                            Margin="0,0,0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding Name}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource TextBrush}"/>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding ConfidenceText}"
                                                       FontSize="10"
                                                       Foreground="{StaticResource DimTextBrush}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Key Selection -->
            <Border Grid.Row="5"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="KEY (for Roman Numeral Analysis)"
                                   Style="{StaticResource SectionLabelStyle}"/>
                        <ComboBox x:Name="KeyComboBox"
                                  Style="{StaticResource ChordDetectorComboBoxStyle}"
                                  ItemsSource="{Binding AvailableKeys}"
                                  SelectedItem="{Binding CurrentKey, Mode=TwoWay}"
                                  Width="150"
                                  HorizontalAlignment="Left"/>
                    </StackPanel>

                    <!-- Sensitivity Slider -->
                    <StackPanel Grid.Column="1" Width="150">
                        <TextBlock Text="SENSITIVITY"
                                   Style="{StaticResource SectionLabelStyle}"/>
                        <Grid>
                            <Slider x:Name="SensitivitySlider"
                                    Style="{StaticResource ChordDetectorSliderStyle}"
                                    Minimum="0"
                                    Maximum="1"
                                    Value="{Binding Sensitivity, Mode=TwoWay}"
                                    TickFrequency="0.1"/>
                        </Grid>
                        <Grid Margin="0,2,0,0">
                            <TextBlock Text="Low"
                                       FontSize="9"
                                       Foreground="{StaticResource DimTextBrush}"
                                       HorizontalAlignment="Left"/>
                            <TextBlock Text="High"
                                       FontSize="9"
                                       Foreground="{StaticResource DimTextBrush}"
                                       HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Chord History -->
            <Border Grid.Row="6"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,10"
                    MinHeight="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <TextBlock Text="CHORD HISTORY"
                                   Style="{StaticResource SectionLabelStyle}"
                                   VerticalAlignment="Center"/>
                        <Button Style="{StaticResource ChordDetectorButtonStyle}"
                                Content="Clear"
                                Command="{Binding ClearHistoryCommand}"
                                HorizontalAlignment="Right"
                                Padding="8,2"
                                FontSize="10"/>
                    </Grid>

                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding ChordHistory}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource PanelBackgroundBrush}"
                                            CornerRadius="3"
                                            Padding="8,4"
                                            Margin="0,0,0,2">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0"
                                                       Text="{Binding TimeText}"
                                                       FontSize="10"
                                                       Foreground="{StaticResource DimTextBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"/>

                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding Name}"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource AccentBrush}"
                                                       VerticalAlignment="Center"/>

                                            <Border Grid.Column="2"
                                                    Background="{DynamicResource BorderBrush}"
                                                    CornerRadius="2"
                                                    Padding="4,1">
                                                <TextBlock Text="{Binding ConfidenceText}"
                                                           FontSize="9"
                                                           Foreground="{StaticResource DimTextBrush}"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- MIDI Output Toggle -->
            <Border Grid.Row="7"
                    Background="{StaticResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0"
                    CornerRadius="0,0,6,6"
                    Padding="12,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <ToggleButton x:Name="MidiOutputToggle"
                                      Style="{StaticResource ChordDetectorToggleStyle}"
                                      Content="MIDI Output"
                                      IsChecked="{Binding IsMidiOutputEnabled, Mode=TwoWay}"
                                      ToolTip="Send detected chord as MIDI notes"/>
                        <TextBlock Text="Send detected chord to MIDI output"
                                   FontSize="10"
                                   Foreground="{StaticResource DimTextBrush}"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0"/>
                    </StackPanel>

                    <TextBlock Grid.Column="1"
                               x:Name="DetectedNotesText"
                               Text=""
                               FontSize="10"
                               Foreground="{StaticResource DimTextBrush}"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
