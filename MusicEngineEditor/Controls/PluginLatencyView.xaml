<UserControl x:Class="MusicEngineEditor.Controls.PluginLatencyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"
             Background="#1E1F22"
             MinWidth="300"
             MinHeight="200">

    <UserControl.Resources>
        <!-- Dark theme colors -->
        <Color x:Key="BackgroundColor">#1E1F22</Color>
        <Color x:Key="PanelColor">#2B2D30</Color>
        <Color x:Key="BorderColor">#393B40</Color>
        <Color x:Key="TextPrimaryColor">#BCBEC4</Color>
        <Color x:Key="TextSecondaryColor">#6F737A</Color>
        <Color x:Key="AccentColor">#4A9EFF</Color>
        <Color x:Key="WarningColor">#FF9500</Color>
        <Color x:Key="ErrorColor">#FF3B30</Color>
        <Color x:Key="SuccessColor">#34C759</Color>
        <Color x:Key="BarBackgroundColor">#1A1B1E</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="BarBackgroundBrush" Color="{StaticResource BarBackgroundColor}"/>

        <!-- PDC Status indicator style -->
        <Style x:Key="StatusIndicatorStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="8"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Plugin Latency"
                               FontSize="14"
                               FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>

                    <!-- PDC Status -->
                    <Border Background="{StaticResource PanelBrush}"
                            CornerRadius="10"
                            Padding="8,3"
                            Margin="12,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="PdcStatusIndicator"
                                     Style="{StaticResource StatusIndicatorStyle}"
                                     Fill="{StaticResource SuccessBrush}"
                                     Margin="0,0,6,0"/>
                            <TextBlock x:Name="PdcStatusText"
                                       Text="PDC Active"
                                       FontSize="10"
                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Total Latency -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Total:"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"/>
                    <TextBlock x:Name="TotalLatencyText"
                               Text="0 samples (0.0 ms)"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <!-- Column Headers -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Track / Plugin"
                           FontSize="10"
                           Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBlock Grid.Column="1"
                           Text="Latency"
                           FontSize="10"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="8,0,0,0"/>
                <TextBlock Grid.Column="2"
                           Text="Samples"
                           FontSize="10"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="3"
                           Text="ms"
                           FontSize="10"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right"/>
            </Grid>

            <!-- Track/Plugin List -->
            <ScrollViewer Grid.Row="2"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="LatencyItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource PanelBrush}"
                                    CornerRadius="4"
                                    Padding="8,6"
                                    Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Track/Plugin Name -->
                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <Rectangle Width="3"
                                                   Height="16"
                                                   Fill="{Binding Color}"
                                                   RadiusX="1"
                                                   RadiusY="1"
                                                   Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <!-- Latency Bar -->
                                    <Grid Grid.Column="1" Margin="8,0">
                                        <Border Background="{StaticResource BarBackgroundBrush}"
                                                CornerRadius="2"
                                                Height="8"/>
                                        <Border Background="{Binding BarColor}"
                                                CornerRadius="2"
                                                Height="8"
                                                HorizontalAlignment="Left"
                                                Width="{Binding BarWidth}"/>
                                    </Grid>

                                    <!-- Samples -->
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding SamplesText}"
                                               FontSize="10"
                                               FontFamily="Consolas"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center"/>

                                    <!-- Milliseconds -->
                                    <TextBlock Grid.Column="3"
                                               Text="{Binding MsText}"
                                               FontSize="10"
                                               FontFamily="Consolas"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer with sample rate info -->
            <Border Grid.Row="3"
                    Background="{StaticResource PanelBrush}"
                    CornerRadius="4"
                    Padding="8,6"
                    Margin="0,8,0,0">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Sample Rate:"
                                   FontSize="10"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="0,0,6,0"/>
                        <TextBlock x:Name="SampleRateText"
                                   Text="44100 Hz"
                                   FontSize="10"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>

                        <TextBlock Text="Buffer:"
                                   FontSize="10"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="16,0,6,0"/>
                        <TextBlock x:Name="BufferSizeText"
                                   Text="512 samples"
                                   FontSize="10"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>

                    <Button x:Name="RefreshButton"
                            Content="Refresh"
                            HorizontalAlignment="Right"
                            Padding="12,3"
                            FontSize="10"
                            Click="OnRefreshClicked"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
