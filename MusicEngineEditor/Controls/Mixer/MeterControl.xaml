<UserControl x:Class="MusicEngineEditor.Controls.Mixer.MeterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="40"
             Background="#0D0D0D">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Channel Meter -->
        <Border Grid.Column="0" BorderBrush="#2A2A2A" BorderThickness="1" Background="#181818">
            <Canvas x:Name="LeftMeterCanvas" ClipToBounds="True"/>
        </Border>

        <!-- Separator -->
        <Rectangle Grid.Column="1" Fill="#2A2A2A"/>

        <!-- Right Channel Meter -->
        <Border Grid.Column="2" BorderBrush="#2A2A2A" BorderThickness="1" Background="#181818">
            <Canvas x:Name="RightMeterCanvas" ClipToBounds="True"/>
        </Border>
    </Grid>
</UserControl>
