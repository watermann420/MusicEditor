<UserControl x:Class="MusicEngineEditor.Controls.EffectsEditorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#0D0D0D">

    <UserControl.Resources>
        <!-- Colors -->
        <Color x:Key="BackgroundColor">#0D0D0D</Color>
        <Color x:Key="PanelColor">#181818</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="TextDimColor">#808080</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="{StaticResource TextDimColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <Path x:Name="icon" Data="M 0,0 L 10,10 M 10,0 L 0,10"
                                  Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="1.5"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Width="10" Height="10"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3C3C3C"/>
                                <Setter TargetName="icon" Property="Stroke" Value="#FF6B6B"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="EffectComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <!-- Toggle Button Style for Bypass -->
        <Style x:Key="BypassToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF6B6B"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF6B6B"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Slider Style -->
        <Style x:Key="EffectSliderStyle" TargetType="Slider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Value" Value="100"/>
            <Setter Property="IsSnapToTickEnabled" Value="True"/>
            <Setter Property="TickFrequency" Value="1"/>
        </Style>

        <!-- Effect Chain ListBox Style -->
        <Style x:Key="EffectChainListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="4"/>
        </Style>

        <Style x:Key="EffectChainItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="12,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Effects Icon -->
                <Border Grid.Column="0" Background="{StaticResource AccentBrush}"
                        CornerRadius="4" Width="32" Height="32" Margin="0,0,12,0">
                    <Path Data="M 4,8 L 8,4 L 12,8 L 8,12 Z M 12,12 L 16,8 L 20,12 L 16,16 Z"
                          Fill="{StaticResource BackgroundBrush}"
                          HorizontalAlignment="Center" VerticalAlignment="Center"
                          Width="20" Height="20"/>
                </Border>

                <!-- Title -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="EFFECTS"
                               Foreground="{StaticResource TextBrush}"
                               FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock x:Name="EffectSubtitle" Text="Audio Effect Processing"
                               Foreground="{StaticResource TextDimBrush}"
                               FontSize="11"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="3" Style="{StaticResource CloseButtonStyle}"
                        Click="CloseButton_Click" ToolTip="Close Effects Editor"/>
            </Grid>
        </Border>

        <!-- Category and Effect Type Selectors -->
        <Border Grid.Row="1" Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Category:"
                           Foreground="{StaticResource TextBrush}"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Column="1" x:Name="CategoryComboBox"
                          Style="{StaticResource EffectComboBoxStyle}"
                          SelectionChanged="CategoryComboBox_SelectionChanged"
                          Margin="0,0,16,0">
                    <ComboBoxItem Content="Dynamics" IsSelected="True"/>
                    <ComboBoxItem Content="Time-Based"/>
                    <ComboBoxItem Content="Modulation"/>
                    <ComboBoxItem Content="Distortion"/>
                    <ComboBoxItem Content="Filters"/>
                    <ComboBoxItem Content="Special"/>
                </ComboBox>

                <TextBlock Grid.Column="2" Text="Effect:"
                           Foreground="{StaticResource TextBrush}"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox Grid.Column="3" x:Name="EffectTypeComboBox"
                          Style="{StaticResource EffectComboBoxStyle}"
                          SelectionChanged="EffectTypeComboBox_SelectionChanged">
                    <!-- Items populated dynamically based on category -->
                </ComboBox>
            </Grid>
        </Border>

        <!-- Effect Control Content Area -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <!-- No Effect Selected State -->
                <Border x:Name="NoEffectPanel" Visibility="Visible">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Path Data="M 20,40 A 20,20 0 1 1 60,40 A 20,20 0 1 1 20,40 M 35,30 L 45,40 L 35,50"
                              Stroke="{StaticResource TextDimBrush}" StrokeThickness="2"
                              Width="80" Height="80" HorizontalAlignment="Center"/>
                        <TextBlock Text="No Effect Selected" Foreground="{StaticResource TextDimBrush}"
                                   FontSize="16" HorizontalAlignment="Center" Margin="0,16,0,8"/>
                        <TextBlock Text="Select a category and effect type to begin editing"
                                   Foreground="{StaticResource TextDimBrush}"
                                   FontSize="12" HorizontalAlignment="Center" TextWrapping="Wrap"
                                   MaxWidth="300" TextAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Dynamic Effect Control Container -->
                <ContentControl x:Name="EffectControlContainer" Visibility="Collapsed"/>
            </Grid>
        </ScrollViewer>

        <!-- Bottom Section: Wet/Dry, Bypass, Effect Chain -->
        <Border Grid.Row="3" Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Wet/Dry and Bypass Controls -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Wet/Dry:"
                               Foreground="{StaticResource TextBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Slider Grid.Column="1" x:Name="WetDrySlider"
                            Style="{StaticResource EffectSliderStyle}"
                            VerticalAlignment="Center" Margin="0,0,8,0"
                            ValueChanged="WetDrySlider_ValueChanged"/>
                    <TextBlock Grid.Column="2" x:Name="WetDryValueText" Text="100%"
                               Foreground="{StaticResource AccentBrush}"
                               VerticalAlignment="Center" Margin="0,0,16,0"
                               MinWidth="40"/>
                    <ToggleButton Grid.Column="3" x:Name="BypassToggle"
                                  Style="{StaticResource BypassToggleStyle}"
                                  Content="BYPASS" Click="BypassToggle_Click"/>
                </Grid>

                <!-- Effect Chain -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Effect Chain"
                               Foreground="{StaticResource TextBrush}"
                               FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ListBox Grid.Row="1" x:Name="EffectChainListBox"
                             Style="{StaticResource EffectChainListBoxStyle}"
                             ItemContainerStyle="{StaticResource EffectChainItemStyle}"
                             MinHeight="80" MaxHeight="150">
                        <!-- Items populated dynamically -->
                    </ListBox>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
