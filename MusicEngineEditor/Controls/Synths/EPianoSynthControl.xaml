<UserControl x:Class="MusicEngineEditor.Controls.Synths.EPianoSynthControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Controls.Synths"
             xmlns:vm="clr-namespace:MusicEngineEditor.ViewModels.Synths"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:EPianoSynthViewModel, IsDesignTimeCreatable=True}"
             Background="#0D0D0D"
             Loaded="EPianoSynthControl_Loaded">

    <UserControl.Resources>
        <!-- Converters -->
        <local:EPianoBoolToColorConverter x:Key="BoolToColorConverter"/>
        <local:EPianoBoolToStatusConverter x:Key="BoolToStatusConverter"/>

        <!-- Dark Theme Colors -->
        <Color x:Key="BackgroundColor">#0D0D0D</Color>
        <Color x:Key="PanelColor">#181818</Color>
        <Color x:Key="PanelLightColor">#1E1E1E</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="AccentSecondaryColor">#00FF88</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="TextDimColor">#808080</Color>
        <Color x:Key="KnobTrackColor">#333333</Color>
        <Color x:Key="RhodesColor">#FFB347</Color>
        <Color x:Key="WurlitzerColor">#FF6B6B</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
        <SolidColorBrush x:Key="PanelLightBrush" Color="{StaticResource PanelLightColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="AccentSecondaryBrush" Color="{StaticResource AccentSecondaryColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="{StaticResource TextDimColor}"/>
        <SolidColorBrush x:Key="KnobTrackBrush" Color="{StaticResource KnobTrackColor}"/>
        <SolidColorBrush x:Key="RhodesBrush" Color="{StaticResource RhodesColor}"/>
        <SolidColorBrush x:Key="WurlitzerBrush" Color="{StaticResource WurlitzerColor}"/>

        <!-- Panel Style -->
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Parameter Label Style -->
        <Style x:Key="ParamLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>

        <!-- Parameter Value Style -->
        <Style x:Key="ParamValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- Slider Style -->
        <Style x:Key="DarkSliderStyle" TargetType="Slider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="1"/>
            <Setter Property="Background" Value="{StaticResource KnobTrackBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Height" Value="20"/>
        </Style>

        <!-- Button Style -->
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="#222222"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#0D0D0D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toggle Button Style -->
        <Style x:Key="ToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#0D0D0D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <!-- Model Button Style -->
        <Style x:Key="ModelButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="#222222"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#0D0D0D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource PanelStyle}" Margin="4,4,4,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ELECTRIC PIANO"
                               Foreground="{StaticResource AccentBrush}"
                               FontSize="14" FontWeight="Bold"/>
                    <TextBlock Text=" - " Foreground="{StaticResource TextDimBrush}"/>
                    <TextBlock Text="{Binding SelectedModelName}"
                               Foreground="{StaticResource TextBrush}"
                               FontSize="12"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Classic" Style="{StaticResource DarkButtonStyle}"
                            Command="{Binding LoadClassicPresetCommand}"
                            ToolTip="Rhodes Classic preset"
                            Margin="0,0,4,0"/>
                    <Button Content="Suitcase" Style="{StaticResource DarkButtonStyle}"
                            Command="{Binding LoadSuitcasePresetCommand}"
                            ToolTip="Rhodes Suitcase with tremolo"
                            Margin="0,0,4,0"/>
                    <Button Content="Wurly" Style="{StaticResource DarkButtonStyle}"
                            Command="{Binding LoadWurlitzerPresetCommand}"
                            ToolTip="Wurlitzer preset"
                            Margin="0,0,4,0"/>
                    <Button Content="Reset" Style="{StaticResource DarkButtonStyle}"
                            Command="{Binding ResetParametersCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Model Selector -->
            <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="PIANO MODEL" Style="{StaticResource SectionHeaderStyle}"/>

                    <!-- Rhodes Models -->
                    <StackPanel Grid.Row="1" Margin="0,0,0,12">
                        <TextBlock Text="Rhodes" Foreground="{StaticResource RhodesBrush}"
                                   FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <RadioButton Content="Mark I" Style="{StaticResource ModelButtonStyle}"
                                     GroupName="Model"
                                     IsChecked="{Binding IsRhodesMarkI}"
                                     Command="{Binding SelectModelCommand}"
                                     CommandParameter="RhodesMarkI"/>
                        <RadioButton Content="Mark II" Style="{StaticResource ModelButtonStyle}"
                                     GroupName="Model"
                                     IsChecked="{Binding IsRhodesMarkII}"
                                     Command="{Binding SelectModelCommand}"
                                     CommandParameter="RhodesMarkII"/>
                        <RadioButton Content="Suitcase" Style="{StaticResource ModelButtonStyle}"
                                     GroupName="Model"
                                     IsChecked="{Binding IsRhodesSuitcase}"
                                     Command="{Binding SelectModelCommand}"
                                     CommandParameter="RhodesSuitcase"/>
                    </StackPanel>

                    <!-- Wurlitzer Models -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,12">
                        <TextBlock Text="Wurlitzer" Foreground="{StaticResource WurlitzerBrush}"
                                   FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <RadioButton Content="200A" Style="{StaticResource ModelButtonStyle}"
                                     GroupName="Model"
                                     IsChecked="{Binding IsWurlitzer}"
                                     Command="{Binding SelectModelCommand}"
                                     CommandParameter="Wurlitzer"/>
                        <RadioButton Content="Electric Grand" Style="{StaticResource ModelButtonStyle}"
                                     GroupName="Model"
                                     IsChecked="{Binding IsElectricGrand}"
                                     Command="{Binding SelectModelCommand}"
                                     CommandParameter="ElectricGrand"/>
                    </StackPanel>

                    <!-- Model Description -->
                    <Border Grid.Row="3" Background="{StaticResource PanelLightBrush}"
                            CornerRadius="3" Padding="8" VerticalAlignment="Top">
                        <TextBlock Text="{Binding ModelDescription}"
                                   Foreground="{StaticResource TextDimBrush}"
                                   FontSize="10"
                                   TextWrapping="Wrap"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Center Panel: Main Controls -->
            <Border Grid.Column="1" Style="{StaticResource PanelStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Tone Section -->
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="TONE" Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <local:KnobControl Grid.Column="0"
                                               Label="Tine/Bar Mix"
                                               Value="{Binding TineBarMix, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               ToolTip="Balance between tine (bright) and tone bar (warm)"/>

                            <local:KnobControl Grid.Column="1"
                                               Label="Bell"
                                               Value="{Binding BellAmount, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               ToolTip="Bell-like overtone amount"/>

                            <local:KnobControl Grid.Column="2"
                                               Label="Bark"
                                               Value="{Binding BarkAmount, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               ToolTip="Growl/bark on hard hits"/>
                        </Grid>
                    </StackPanel>

                    <!-- Decay Section -->
                    <StackPanel Grid.Row="1" Margin="0,16,0,0">
                        <TextBlock Text="ENVELOPE" Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <Grid>
                                    <TextBlock Text="Decay Time" Style="{StaticResource ParamLabelStyle}"
                                               HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding DecayTime, StringFormat={}{0:F2}s}"
                                               Style="{StaticResource ParamValueStyle}"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider Value="{Binding DecayTime}" Minimum="0.5" Maximum="10"
                                        Style="{StaticResource DarkSliderStyle}"
                                        ToolTip="Note decay time"/>
                            </StackPanel>

                            <local:KnobControl Grid.Column="1"
                                               Label="Volume"
                                               Value="{Binding Volume, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"/>
                        </Grid>
                    </StackPanel>

                    <!-- Tremolo Section -->
                    <StackPanel Grid.Row="2" Margin="0,16,0,0">
                        <Grid>
                            <TextBlock Text="TREMOLO" Style="{StaticResource SectionHeaderStyle}"/>
                            <ToggleButton Content="On" Style="{StaticResource ToggleButtonStyle}"
                                          IsChecked="{Binding TremoloEnabled}"
                                          HorizontalAlignment="Right"
                                          Margin="0,-4,0,0"
                                          Width="40"/>
                        </Grid>
                        <Grid Margin="0,4,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <local:KnobControl Grid.Column="0"
                                               Label="Rate"
                                               Value="{Binding TremoloRate, Mode=TwoWay}"
                                               Minimum="0.1" Maximum="20"
                                               ValueFormat="F1"
                                               IsEnabled="{Binding TremoloEnabled}"
                                               ToolTip="Tremolo speed in Hz"/>

                            <local:KnobControl Grid.Column="1"
                                               Label="Depth"
                                               Value="{Binding TremoloDepth, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               IsEnabled="{Binding TremoloEnabled}"
                                               ToolTip="Tremolo intensity"/>

                            <local:KnobControl Grid.Column="2"
                                               Label="Stereo"
                                               Value="{Binding StereoWidth, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               IsEnabled="{Binding TremoloEnabled}"
                                               ToolTip="Stereo spread of tremolo"/>
                        </Grid>
                    </StackPanel>

                    <!-- Drive Section -->
                    <StackPanel Grid.Row="3" Margin="0,16,0,0">
                        <TextBlock Text="DRIVE" Style="{StaticResource SectionHeaderStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <Grid>
                                    <TextBlock Text="Overdrive" Style="{StaticResource ParamLabelStyle}"
                                               HorizontalAlignment="Left"/>
                                    <TextBlock Text="{Binding Drive, StringFormat={}{0:P0}}"
                                               Style="{StaticResource ParamValueStyle}"
                                               HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider Value="{Binding Drive}" Minimum="0" Maximum="1"
                                        Style="{StaticResource DarkSliderStyle}"
                                        ToolTip="Tube-style overdrive/saturation"/>
                            </StackPanel>

                            <Border Grid.Column="1" Background="{StaticResource PanelLightBrush}"
                                    CornerRadius="3" Padding="8" VerticalAlignment="Center">
                                <TextBlock Text="Adds warmth and grit to the sound"
                                           Foreground="{StaticResource TextDimBrush}"
                                           FontSize="9" TextWrapping="Wrap"/>
                            </Border>
                        </Grid>
                    </StackPanel>

                    <!-- Waveform Visualization -->
                    <Border Grid.Row="4"
                            Background="#0A0A0A"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Margin="0,16,0,0"
                            ClipToBounds="True">
                        <Canvas x:Name="WaveformCanvas"
                                SizeChanged="WaveformCanvas_SizeChanged"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Right Panel: EQ and Effects -->
            <Border Grid.Column="2" Style="{StaticResource PanelStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- EQ Section -->
                        <TextBlock Text="EQUALIZER" Style="{StaticResource SectionHeaderStyle}"/>
                        <UniformGrid Columns="2" Margin="0,0,0,16">
                            <local:KnobControl Label="Bass"
                                               Value="{Binding Bass, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               ToolTip="Low frequency boost/cut"/>
                            <local:KnobControl Label="Treble"
                                               Value="{Binding Treble, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               ToolTip="High frequency boost/cut"/>
                        </UniformGrid>

                        <!-- Chorus Section -->
                        <Grid>
                            <TextBlock Text="CHORUS" Style="{StaticResource SectionHeaderStyle}"/>
                            <ToggleButton Content="On" Style="{StaticResource ToggleButtonStyle}"
                                          IsChecked="{Binding ChorusEnabled}"
                                          HorizontalAlignment="Right"
                                          Margin="0,-4,0,0"
                                          Width="40"/>
                        </Grid>
                        <UniformGrid Columns="2" Margin="0,4,0,16">
                            <local:KnobControl Label="Rate"
                                               Value="{Binding ChorusRate, Mode=TwoWay}"
                                               Minimum="0.1" Maximum="5"
                                               ValueFormat="F1"
                                               IsEnabled="{Binding ChorusEnabled}"/>
                            <local:KnobControl Label="Depth"
                                               Value="{Binding ChorusDepth, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               IsEnabled="{Binding ChorusEnabled}"/>
                        </UniformGrid>
                        <StackPanel Margin="0,0,0,16">
                            <Grid>
                                <TextBlock Text="Mix" Style="{StaticResource ParamLabelStyle}"
                                           HorizontalAlignment="Left"/>
                                <TextBlock Text="{Binding ChorusMix, StringFormat={}{0:P0}}"
                                           Style="{StaticResource ParamValueStyle}"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Slider Value="{Binding ChorusMix}" Minimum="0" Maximum="1"
                                    Style="{StaticResource DarkSliderStyle}"
                                    IsEnabled="{Binding ChorusEnabled}"/>
                        </StackPanel>

                        <!-- Phaser Section -->
                        <Grid>
                            <TextBlock Text="PHASER" Style="{StaticResource SectionHeaderStyle}"/>
                            <ToggleButton Content="On" Style="{StaticResource ToggleButtonStyle}"
                                          IsChecked="{Binding PhaserEnabled}"
                                          HorizontalAlignment="Right"
                                          Margin="0,-4,0,0"
                                          Width="40"/>
                        </Grid>
                        <UniformGrid Columns="2" Margin="0,4,0,8">
                            <local:KnobControl Label="Rate"
                                               Value="{Binding PhaserRate, Mode=TwoWay}"
                                               Minimum="0.1" Maximum="5"
                                               ValueFormat="F1"
                                               IsEnabled="{Binding PhaserEnabled}"/>
                            <local:KnobControl Label="Depth"
                                               Value="{Binding PhaserDepth, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               IsEnabled="{Binding PhaserEnabled}"/>
                        </UniformGrid>
                        <UniformGrid Columns="2" Margin="0,0,0,8">
                            <local:KnobControl Label="Feedback"
                                               Value="{Binding PhaserFeedback, Mode=TwoWay}"
                                               Minimum="0" Maximum="0.95"
                                               IsEnabled="{Binding PhaserEnabled}"/>
                            <local:KnobControl Label="Mix"
                                               Value="{Binding PhaserMix, Mode=TwoWay}"
                                               Minimum="0" Maximum="1"
                                               IsEnabled="{Binding PhaserEnabled}"/>
                        </UniformGrid>

                        <!-- Quick Presets -->
                        <TextBlock Text="QUICK PRESETS" Style="{StaticResource SectionHeaderStyle}" Margin="0,8,0,8"/>
                        <WrapPanel>
                            <Button Content="Ballad" Style="{StaticResource DarkButtonStyle}" Padding="8,4"
                                    Command="{Binding LoadBalladPresetCommand}" Margin="2"
                                    ToolTip="Soft ballad Rhodes"/>
                            <Button Content="Driven" Style="{StaticResource DarkButtonStyle}" Padding="8,4"
                                    Command="{Binding LoadDrivenPresetCommand}" Margin="2"
                                    ToolTip="Overdriven Rhodes with phaser"/>
                            <Button Content="Grand" Style="{StaticResource DarkButtonStyle}" Padding="8,4"
                                    Command="{Binding LoadGrandPresetCommand}" Margin="2"
                                    ToolTip="Electric grand piano"/>
                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Bottom Status Bar -->
        <Border Grid.Row="2" Style="{StaticResource PanelStyle}" Margin="4,0,4,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{StaticResource TextDimBrush}"
                           FontSize="10"
                           VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0">
                    <Run Text="Voices: " Foreground="{StaticResource TextDimBrush}"/>
                    <Run Text="{Binding ActiveVoices}" Foreground="{StaticResource TextBrush}"/>
                    <Run Text="/" Foreground="{StaticResource TextDimBrush}"/>
                    <Run Text="{Binding MaxVoices}" Foreground="{StaticResource TextBrush}"/>
                </TextBlock>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Ellipse Width="8" Height="8"
                             Fill="{Binding IsPlaying, Converter={StaticResource BoolToColorConverter}, FallbackValue=#333333}"
                             Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding IsPlaying, Converter={StaticResource BoolToStatusConverter}, FallbackValue=Idle}"
                               Foreground="{StaticResource TextDimBrush}"
                               FontSize="10"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
