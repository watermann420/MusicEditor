<UserControl x:Class="MusicEngineEditor.Controls.PresetBrowserPanel"
             x:Name="PresetBrowserPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="350"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Colors matching the dark theme -->
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#0D0D0D"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#181818"/>
        <SolidColorBrush x:Key="CardHoverBrush" Color="#252525"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="ForegroundBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="SecondaryForegroundBrush" Color="#808080"/>
        <SolidColorBrush x:Key="BrightForegroundBrush" Color="#DFE1E5"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00D9FF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#5A7FC4"/>
        <SolidColorBrush x:Key="FavoriteBrush" Color="#FFD700"/>
        <SolidColorBrush x:Key="SynthColor" Color="#9C7CE8"/>
        <SolidColorBrush x:Key="EffectColor" Color="#E89C4B"/>
        <SolidColorBrush x:Key="PatternColor" Color="#4BE89C"/>
        <SolidColorBrush x:Key="GrooveColor" Color="#E84B9C"/>

        <!-- Search TextBox Style -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="&#x1F50D;"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryForegroundBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,4,0"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Grid.Column="1"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="placeholder"
                                           Grid.Column="1"
                                           Text="Search presets..."
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryForegroundBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                                <Button x:Name="clearButton"
                                        Grid.Column="2"
                                        Content="X"
                                        FontSize="10"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{StaticResource SecondaryForegroundBrush}"
                                        Padding="8,0"
                                        Cursor="Hand"
                                        Visibility="Collapsed"
                                        Click="ClearSearch_Click"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource Self}}" Value="0">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="clearButton" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="clearButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter Button Style -->
        <Style x:Key="FilterButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tag Button Style -->
        <Style x:Key="TagButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tree View Item Style -->
        <Style x:Key="TreeViewItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border x:Name="Bd"
                                    Background="Transparent"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="3"
                                    Margin="0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ToggleButton x:Name="Expander"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  Width="16" Height="16"
                                                  Margin="0,0,4,0"
                                                  Background="Transparent"
                                                  BorderThickness="0">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <Path x:Name="Arrow"
                                                          Data="M 0,0 L 4,4 L 0,8 Z"
                                                          Fill="{StaticResource SecondaryForegroundBrush}"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Arrow" Property="Data" Value="M 0,0 L 8,0 L 4,4 Z"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                    <ContentPresenter Grid.Column="1"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Left"/>
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost"
                                            Grid.Row="1"
                                            Margin="16,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#252525"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Preset List Item Style -->
        <Style x:Key="PresetListItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#252525"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Preview Button Style -->
        <Style x:Key="PreviewButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Width="24" Height="24">
                            <TextBlock x:Name="icon"
                                       Text="&#x25B6;"
                                       FontSize="10"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="icon" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Playing">
                                <Setter TargetName="border" Property="Background" Value="#00FF88"/>
                                <Setter TargetName="icon" Property="Text" Value="&#x25A0;"/>
                                <Setter TargetName="icon" Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Favorite Button Style -->
        <Style x:Key="FavoriteButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="Transparent">
                            <TextBlock x:Name="star"
                                       Text="&#x2606;"
                                       FontSize="14"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="star" Property="Foreground" Value="{StaticResource FavoriteBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="star" Property="Text" Value="&#x2605;"/>
                                <Setter TargetName="star" Property="Foreground" Value="{StaticResource FavoriteBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <Border Grid.Row="0" Padding="8">
            <TextBox x:Name="SearchTextBox"
                     Style="{StaticResource SearchTextBoxStyle}"
                     TextChanged="SearchTextBox_TextChanged"/>
        </Border>

        <!-- Filter Buttons -->
        <Border Grid.Row="1" Padding="8,0,8,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Type Filters -->
                <WrapPanel Grid.Column="0">
                    <ToggleButton x:Name="AllFilterButton"
                                  Content="All"
                                  Style="{StaticResource FilterButtonStyle}"
                                  IsChecked="True"
                                  Click="FilterButton_Click"
                                  Margin="0,0,4,0"/>
                    <ToggleButton x:Name="SynthsFilterButton"
                                  Content="Synths"
                                  Style="{StaticResource FilterButtonStyle}"
                                  Click="FilterButton_Click"
                                  Margin="0,0,4,0"/>
                    <ToggleButton x:Name="EffectsFilterButton"
                                  Content="Effects"
                                  Style="{StaticResource FilterButtonStyle}"
                                  Click="FilterButton_Click"
                                  Margin="0,0,4,0"/>
                    <ToggleButton x:Name="PatternsFilterButton"
                                  Content="Patterns"
                                  Style="{StaticResource FilterButtonStyle}"
                                  Click="FilterButton_Click"
                                  Margin="0,0,4,0"/>
                </WrapPanel>

                <!-- Favorites Filter -->
                <ToggleButton x:Name="FavoritesFilterButton"
                              Grid.Column="1"
                              Content="&#x2665;"
                              Style="{StaticResource FilterButtonStyle}"
                              ToolTip="Show Favorites Only"
                              Click="FavoritesFilter_Click"/>
            </Grid>
        </Border>

        <!-- Tags Filter -->
        <Border Grid.Row="2" Padding="8,0,8,8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" MaxHeight="30">
                <StackPanel x:Name="TagsPanel" Orientation="Horizontal">
                    <ToggleButton Content="Bass" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="Lead" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="Pad" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="Drums" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="FX" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="Keys" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="Pluck" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                    <ToggleButton Content="Ambient" Style="{StaticResource TagButtonStyle}" Click="TagButton_Click"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Main Content - Split View -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140" MinWidth="100"/>
                <ColumnDefinition Width="1"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side - Tree View -->
            <Border Grid.Column="0" Background="{DynamicResource PanelBackgroundBrush}">
                <TreeView x:Name="CategoryTreeView"
                          Background="Transparent"
                          BorderThickness="0"
                          Padding="4"
                          SelectedItemChanged="CategoryTreeView_SelectedItemChanged"
                          ItemContainerStyle="{StaticResource TreeViewItemStyle}">
                    <TreeViewItem Header="Synths" IsExpanded="True">
                        <TreeViewItem Header="Simple"/>
                        <TreeViewItem Header="Poly"/>
                        <TreeViewItem Header="FM"/>
                        <TreeViewItem Header="Wavetable"/>
                        <TreeViewItem Header="Sampler"/>
                    </TreeViewItem>
                    <TreeViewItem Header="Effects" IsExpanded="True">
                        <TreeViewItem Header="Reverb"/>
                        <TreeViewItem Header="Delay"/>
                        <TreeViewItem Header="Distortion"/>
                        <TreeViewItem Header="Filter"/>
                        <TreeViewItem Header="Modulation"/>
                    </TreeViewItem>
                    <TreeViewItem Header="Patterns"/>
                    <TreeViewItem Header="Grooves"/>
                    <TreeViewItem Header="Favorites">
                        <TreeViewItem.HeaderTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#x2605;" Foreground="#FFD700" Margin="0,0,4,0"/>
                                    <TextBlock Text="Favorites"/>
                                </StackPanel>
                            </DataTemplate>
                        </TreeViewItem.HeaderTemplate>
                    </TreeViewItem>
                </TreeView>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="1" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}"/>

            <!-- Right Side - Preset List -->
            <Border Grid.Column="2" Background="{DynamicResource BackgroundBrush}">
                <ListView x:Name="PresetListView"
                          Background="Transparent"
                          BorderThickness="0"
                          Padding="4"
                          ItemContainerStyle="{StaticResource PresetListItemStyle}"
                          SelectionChanged="PresetListView_SelectionChanged"
                          MouseDoubleClick="PresetListView_MouseDoubleClick"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Preview Button -->
                                <Button Grid.Column="0"
                                        Style="{StaticResource PreviewButtonStyle}"
                                        Click="Preview_Click"
                                        Margin="0,0,6,0"/>

                                <!-- Preset Info -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="12"
                                               FontWeight="Medium"
                                               Foreground="{StaticResource ForegroundBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding TargetTypeDisplay}"
                                                   FontSize="9"
                                                   Foreground="{StaticResource SecondaryForegroundBrush}"
                                                   Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding TagsDisplay}"
                                                   FontSize="9"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxWidth="100"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Type Badge -->
                                <Border Grid.Column="2"
                                        Background="{DynamicResource BorderBrush}"
                                        CornerRadius="3"
                                        Padding="4,2"
                                        Margin="4,0"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Category}"
                                               FontSize="9"
                                               Foreground="{StaticResource SecondaryForegroundBrush}"/>
                                </Border>

                                <!-- Favorite Button -->
                                <ToggleButton Grid.Column="3"
                                              Style="{StaticResource FavoriteButtonStyle}"
                                              IsChecked="{Binding IsFavorite}"
                                              Click="Favorite_Click"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>

        <!-- Preview Player Controls -->
        <Border Grid.Row="4"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Play/Stop Preview Button -->
                <Button x:Name="PreviewPlayButton"
                        Grid.Column="0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Content="&#x25B6; Preview"
                        Click="PreviewPlayButton_Click"
                        IsEnabled="False"
                        Margin="0,0,8,0"/>

                <!-- Preview Progress -->
                <ProgressBar x:Name="PreviewProgressBar"
                             Grid.Column="1"
                             Height="4"
                             Background="{DynamicResource BorderBrush}"
                             Foreground="{StaticResource AccentBrush}"
                             Value="0"
                             Maximum="100"
                             VerticalAlignment="Center"/>

                <!-- Preview Volume -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,0">
                    <TextBlock Text="&#x1F50A;" FontSize="12" Foreground="{StaticResource SecondaryForegroundBrush}" VerticalAlignment="Center"/>
                    <Slider x:Name="PreviewVolumeSlider"
                            Width="60"
                            Minimum="0"
                            Maximum="100"
                            Value="75"
                            VerticalAlignment="Center"
                            Margin="4,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Bottom Panel - Info and Load Button -->
        <Border Grid.Row="5"
                Background="{DynamicResource BackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Preset Info -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="SelectedPresetName"
                                   Text="No preset selected"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource BrightForegroundBrush}"
                                   TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="SelectedPresetAuthor"
                                   Text=""
                                   FontSize="10"
                                   Foreground="{StaticResource SecondaryForegroundBrush}"
                                   Margin="0,2,0,0"/>
                        <TextBlock x:Name="SelectedPresetDescription"
                                   Text=""
                                   FontSize="10"
                                   Foreground="{StaticResource SecondaryForegroundBrush}"
                                   TextWrapping="Wrap"
                                   MaxHeight="40"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <TextBlock x:Name="SelectedPresetDate"
                               Grid.Column="1"
                               Text=""
                               FontSize="9"
                               Foreground="{StaticResource SecondaryForegroundBrush}"
                               VerticalAlignment="Top"/>
                </Grid>

                <!-- Load Button -->
                <Button x:Name="LoadButton"
                        Grid.Row="1"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Content="Load Preset"
                        Click="LoadButton_Click"
                        IsEnabled="False"
                        HorizontalAlignment="Stretch"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
