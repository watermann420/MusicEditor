<UserControl x:Class="MusicEngineEditor.Controls.GainStagingMeter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="250"
             Background="#1E1F22"
             MinHeight="50"
             MinWidth="150">

    <UserControl.Resources>
        <!-- Dark theme colors -->
        <Color x:Key="MeterBackgroundColor">#1E1F22</Color>
        <Color x:Key="MeterTrackColor">#0D0D0D</Color>
        <Color x:Key="MeterBorderColor">#393B40</Color>
        <Color x:Key="TextPrimaryColor">#BCBEC4</Color>
        <Color x:Key="TextSecondaryColor">#6F737A</Color>
        <Color x:Key="TargetLineColor">#00BFFF</Color>

        <!-- Level gradient colors -->
        <Color x:Key="LevelGreenColor">#00FF00</Color>
        <Color x:Key="LevelYellowColor">#FFFF00</Color>
        <Color x:Key="LevelOrangeColor">#FF8800</Color>
        <Color x:Key="LevelRedColor">#FF0000</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="MeterBackgroundBrush" Color="{StaticResource MeterBackgroundColor}"/>
        <SolidColorBrush x:Key="MeterTrackBrush" Color="{StaticResource MeterTrackColor}"/>
        <SolidColorBrush x:Key="MeterBorderBrush" Color="{StaticResource MeterBorderColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="TargetLineBrush" Color="{StaticResource TargetLineColor}"/>

        <!-- Horizontal level gradient (left to right: green -> yellow -> orange -> red) -->
        <LinearGradientBrush x:Key="LevelGradientBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="{StaticResource LevelGreenColor}" Offset="0.0"/>
            <GradientStop Color="{StaticResource LevelGreenColor}" Offset="0.6"/>
            <GradientStop Color="{StaticResource LevelYellowColor}" Offset="0.75"/>
            <GradientStop Color="{StaticResource LevelOrangeColor}" Offset="0.9"/>
            <GradientStop Color="{StaticResource LevelRedColor}" Offset="1.0"/>
        </LinearGradientBrush>
    </UserControl.Resources>

    <Border Background="{StaticResource MeterBackgroundBrush}"
            BorderBrush="{StaticResource MeterBorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="8,4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Gain Staging"
                       FontSize="10"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,4"/>

            <!-- Meters Area -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="4"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Pre-Fader Meter -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <!-- Label -->
                    <TextBlock Grid.Column="0"
                               Text="Pre-Fader"
                               FontSize="9"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>

                    <!-- Meter Track -->
                    <Border Grid.Column="1"
                            Background="{StaticResource MeterTrackBrush}"
                            BorderBrush="{StaticResource MeterBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="2"
                            Margin="4,2"
                            ClipToBounds="True">
                        <Grid>
                            <!-- Level Bar -->
                            <Border x:Name="PreFaderBar"
                                    Background="{StaticResource LevelGradientBrush}"
                                    HorizontalAlignment="Left"
                                    CornerRadius="1"
                                    Margin="1"/>

                            <!-- Target Level Line -->
                            <Rectangle x:Name="PreTargetLine"
                                       Width="2"
                                       Fill="{StaticResource TargetLineBrush}"
                                       HorizontalAlignment="Left"
                                       Margin="0,1"
                                       Opacity="0.8"/>
                        </Grid>
                    </Border>

                    <!-- Level Display -->
                    <TextBlock x:Name="PreFaderDbText"
                               Grid.Column="2"
                               Text="-inf dB"
                               FontSize="9"
                               FontFamily="Consolas"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </Grid>

                <!-- Arrow/Flow Indicator -->
                <TextBlock Grid.Row="1"
                           Text=">"
                           FontSize="8"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <!-- Post-Fader Meter -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40"/>
                    </Grid.ColumnDefinitions>

                    <!-- Label -->
                    <TextBlock Grid.Column="0"
                               Text="Post-Fader"
                               FontSize="9"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"/>

                    <!-- Meter Track -->
                    <Border Grid.Column="1"
                            Background="{StaticResource MeterTrackBrush}"
                            BorderBrush="{StaticResource MeterBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="2"
                            Margin="4,2"
                            ClipToBounds="True">
                        <Grid>
                            <!-- Level Bar -->
                            <Border x:Name="PostFaderBar"
                                    Background="{StaticResource LevelGradientBrush}"
                                    HorizontalAlignment="Left"
                                    CornerRadius="1"
                                    Margin="1"/>

                            <!-- Target Level Line -->
                            <Rectangle x:Name="PostTargetLine"
                                       Width="2"
                                       Fill="{StaticResource TargetLineBrush}"
                                       HorizontalAlignment="Left"
                                       Margin="0,1"
                                       Opacity="0.8"/>
                        </Grid>
                    </Border>

                    <!-- Level Display -->
                    <TextBlock x:Name="PostFaderDbText"
                               Grid.Column="2"
                               Text="-inf dB"
                               FontSize="9"
                               FontFamily="Consolas"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </Grid>
            </Grid>

            <!-- Scale Labels -->
            <Grid Grid.Row="2" Margin="54,2,40,0">
                <TextBlock Text="-60"
                           FontSize="8"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Left"/>
                <TextBlock Text="-18"
                           FontSize="8"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           Margin="-20,0,0,0"/>
                <TextBlock Text="0 dB"
                           FontSize="8"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
