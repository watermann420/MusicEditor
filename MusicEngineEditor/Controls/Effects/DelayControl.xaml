<UserControl x:Class="MusicEngineEditor.Controls.Effects.DelayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="400"
             Background="#0D0D0D">

    <UserControl.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#0D0D0D"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#181818"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00D9FF"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="#808080"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2A2A2A"/>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Value Style -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="MinWidth" Value="60"/>
        </Style>

        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Panel Style -->
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="4"/>
        </Style>

        <!-- Slider Style -->
        <Style x:Key="ParamSliderStyle" TargetType="Slider">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>

        <!-- Toggle Button Style -->
        <Style x:Key="ToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#0D0D0D"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource PanelStyle}" Margin="0">
        <StackPanel>
            <TextBlock Text="DELAY" Style="{StaticResource HeaderStyle}"/>

            <!-- Time -->
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Time:" Style="{StaticResource LabelStyle}"/>
                <Slider Grid.Column="1" x:Name="TimeSlider"
                        Value="{Binding Time, Mode=TwoWay, FallbackValue=250}"
                        Minimum="1" Maximum="2000" Style="{StaticResource ParamSliderStyle}"
                        ValueChanged="TimeSlider_ValueChanged"/>
                <TextBlock Grid.Column="2" x:Name="TimeValue" Text="250 ms"
                           Style="{StaticResource ValueStyle}"/>
            </Grid>

            <!-- Feedback -->
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Feedback:" Style="{StaticResource LabelStyle}"/>
                <Slider Grid.Column="1" x:Name="FeedbackSlider"
                        Value="{Binding Feedback, Mode=TwoWay, FallbackValue=40}"
                        Minimum="0" Maximum="100" Style="{StaticResource ParamSliderStyle}"
                        ValueChanged="FeedbackSlider_ValueChanged"/>
                <TextBlock Grid.Column="2" x:Name="FeedbackValue" Text="40%"
                           Style="{StaticResource ValueStyle}"/>
            </Grid>

            <!-- Mix -->
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Mix:" Style="{StaticResource LabelStyle}"/>
                <Slider Grid.Column="1" x:Name="MixSlider"
                        Value="{Binding Mix, Mode=TwoWay, FallbackValue=30}"
                        Minimum="0" Maximum="100" Style="{StaticResource ParamSliderStyle}"
                        ValueChanged="MixSlider_ValueChanged"/>
                <TextBlock Grid.Column="2" x:Name="MixValue" Text="30%"
                           Style="{StaticResource ValueStyle}"/>
            </Grid>

            <!-- Toggle Buttons -->
            <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ToggleButton Grid.Column="0" x:Name="SyncToggle"
                              Content="Sync"
                              Style="{StaticResource ToggleStyle}"
                              Click="SyncToggle_Click"/>

                <ToggleButton Grid.Column="2" x:Name="PingPongToggle"
                              Content="Ping-Pong"
                              Style="{StaticResource ToggleStyle}"
                              Click="PingPongToggle_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
