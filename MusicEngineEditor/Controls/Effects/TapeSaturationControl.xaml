<UserControl x:Class="MusicEngineEditor.Controls.Effects.TapeSaturationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="340"
             Background="#0D0D0D">

    <UserControl.Resources>
        <!-- Colors -->
        <Color x:Key="BackgroundColor">#0D0D0D</Color>
        <Color x:Key="PanelColor">#181818</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="TextDimColor">#808080</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="{StaticResource TextDimColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Value Style -->
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="MinWidth" Value="60"/>
        </Style>

        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
        </Style>

        <!-- Panel Style -->
        <Style x:Key="PanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <!-- Slider Style -->
        <Style x:Key="TapeSliderStyle" TargetType="Slider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border x:Name="TrackBackground"
                                    Height="4"
                                    Background="{StaticResource BorderBrush}"
                                    CornerRadius="2"
                                    VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4"
                                                        Background="{StaticResource AccentBrush}"
                                                        CornerRadius="2,0,0,2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="14" Height="14"
                                                         Fill="{StaticResource AccentBrush}"
                                                         Stroke="{StaticResource BackgroundBrush}"
                                                         StrokeThickness="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Speed Radio Button Style -->
        <Style x:Key="SpeedRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#0D0D0D"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource PanelStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="TAPE SATURATION" Style="{StaticResource HeaderStyle}" Margin="0,0,0,12"/>

            <!-- Tape Speed Selector -->
            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <TextBlock Text="TAPE SPEED" Style="{StaticResource SectionHeaderStyle}" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="Speed7_5Radio" Content="7.5 IPS" Style="{StaticResource SpeedRadioStyle}"
                                 Checked="SpeedRadio_Checked"/>
                    <RadioButton x:Name="Speed15Radio" Content="15 IPS" Style="{StaticResource SpeedRadioStyle}"
                                 IsChecked="True" Checked="SpeedRadio_Checked"/>
                    <RadioButton x:Name="Speed30Radio" Content="30 IPS" Style="{StaticResource SpeedRadioStyle}"
                                 Checked="SpeedRadio_Checked"/>
                </StackPanel>
            </StackPanel>

            <!-- Input Gain -->
            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Input Gain" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="InputGainValue" Text="0.0 dB" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="InputGainSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Minimum="-24" Maximum="24" Value="0"
                        ValueChanged="InputGainSlider_ValueChanged"
                        ToolTip="Input gain before saturation"/>
            </StackPanel>

            <!-- Saturation Amount -->
            <StackPanel Grid.Row="3" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Saturation" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="SaturationValue" Text="40%" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="SaturationSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Value="40"
                        ValueChanged="SaturationSlider_ValueChanged"
                        ToolTip="Amount of tape saturation"/>
            </StackPanel>

            <!-- Bias -->
            <StackPanel Grid.Row="4" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Bias" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="BiasValue" Text="50%" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="BiasSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Value="50"
                        ValueChanged="BiasSlider_ValueChanged"
                        ToolTip="Tape bias adjustment"/>
            </StackPanel>

            <!-- High Frequency Rolloff -->
            <StackPanel Grid.Row="5" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="HF Rolloff" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="HFRolloffValue" Text="30%" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="HFRolloffSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Value="30"
                        ValueChanged="HFRolloffSlider_ValueChanged"
                        ToolTip="High frequency rolloff amount"/>
            </StackPanel>

            <!-- Wow & Flutter -->
            <StackPanel Grid.Row="6" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Wow/Flutter" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="WowFlutterValue" Text="15%" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="WowFlutterSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Value="15"
                        ValueChanged="WowFlutterSlider_ValueChanged"
                        ToolTip="Amount of pitch wobble from tape imperfections"/>
            </StackPanel>

            <!-- Noise Floor -->
            <StackPanel Grid.Row="7" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Noise Floor" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="NoiseFloorValue" Text="-70 dB" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="NoiseFloorSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Minimum="-96" Maximum="-30" Value="-70"
                        ValueChanged="NoiseFloorSlider_ValueChanged"
                        ToolTip="Tape noise floor level"/>
            </StackPanel>

            <!-- Hiss Amount -->
            <StackPanel Grid.Row="8" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Hiss" Style="{StaticResource LabelStyle}"/>
                    <TextBlock Grid.Column="1" x:Name="HissValue" Text="20%" Style="{StaticResource ValueStyle}"/>
                </Grid>
                <Slider x:Name="HissSlider"
                        Style="{StaticResource TapeSliderStyle}"
                        Value="20"
                        ValueChanged="HissSlider_ValueChanged"
                        ToolTip="Amount of tape hiss"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
