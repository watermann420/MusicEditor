<UserControl x:Class="MusicEngineEditor.Controls.Arrangement.AudioClipControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="200"
             ClipToBounds="True"
             Cursor="Hand">

    <UserControl.Resources>
        <!-- Theme Colors -->
        <Color x:Key="BackgroundColor">#0D0D0D</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>
        <Color x:Key="PanelColor">#181818</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>

        <!-- Audio Clip Default Color -->
        <Color x:Key="AudioClipColor">#00CC66</Color>
        <Color x:Key="AudioClipFillColor">#B000CC66</Color>
        <SolidColorBrush x:Key="AudioClipBrush" Color="{StaticResource AudioClipColor}"/>
        <SolidColorBrush x:Key="AudioClipFillBrush" Color="{StaticResource AudioClipFillColor}"/>
        <SolidColorBrush x:Key="WaveformBrush" Color="#FFFFFF"/>

        <!-- Clip Border Style -->
        <Style x:Key="ClipBorderStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.3" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Resize Handle Style -->
        <Style x:Key="ResizeHandleStyle" TargetType="Border">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="SizeWE"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#40FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid">
        <!-- Main Clip Border -->
        <Border x:Name="ClipBorder"
                Style="{StaticResource ClipBorderStyle}"
                Background="{StaticResource AudioClipFillBrush}"
                BorderBrush="{StaticResource AudioClipBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header Bar with Clip Name -->
                <Border x:Name="HeaderBar"
                        Grid.Row="0"
                        Background="{StaticResource AudioClipBrush}"
                        CornerRadius="4,4,0,0"
                        Padding="6,2">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Audio Icon -->
                        <TextBlock Grid.Column="0"
                                   Text="~"
                                   FontSize="10"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="0,0,4,0"
                                   ToolTip="Audio Clip"/>

                        <!-- Clip Name Label -->
                        <TextBlock x:Name="ClipNameLabel"
                                   Grid.Column="1"
                                   Text="Audio Clip"
                                   FontSize="10"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"/>
                    </Grid>
                </Border>

                <!-- Waveform Preview Canvas -->
                <Canvas x:Name="WaveformCanvas"
                        Grid.Row="1"
                        Background="Transparent"
                        ClipToBounds="True">

                    <!-- Waveform Polyline -->
                    <Polyline x:Name="WaveformPolyline"
                              Stroke="{StaticResource WaveformBrush}"
                              StrokeThickness="1"
                              Opacity="0.8"/>

                    <!-- Center Line -->
                    <Line x:Name="CenterLine"
                          Stroke="#30FFFFFF"
                          StrokeThickness="1"
                          IsHitTestVisible="False"/>
                </Canvas>

                <!-- Selection Highlight -->
                <Border x:Name="SelectionBorder"
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        BorderBrush="{StaticResource AccentBrush}"
                        BorderThickness="2"
                        CornerRadius="4"
                        Visibility="Collapsed"
                        IsHitTestVisible="False"/>
            </Grid>
        </Border>

        <!-- Left Resize Handle -->
        <Border x:Name="LeftResizeHandle"
                Style="{StaticResource ResizeHandleStyle}"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                MouseLeftButtonDown="LeftResizeHandle_MouseDown"/>

        <!-- Right Resize Handle -->
        <Border x:Name="RightResizeHandle"
                Style="{StaticResource ResizeHandleStyle}"
                HorizontalAlignment="Right"
                VerticalAlignment="Stretch"
                MouseLeftButtonDown="RightResizeHandle_MouseDown"/>

        <!-- Context Menu -->
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Edit in Audio Editor" Click="EditMenuItem_Click"/>
                <Separator/>
                <MenuItem Header="Duplicate" Click="DuplicateMenuItem_Click"/>
                <MenuItem Header="Delete" Click="DeleteMenuItem_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>
