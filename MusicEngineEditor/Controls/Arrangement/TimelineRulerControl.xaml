<UserControl x:Class="MusicEngineEditor.Controls.Arrangement.TimelineRulerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="800"
             Background="#0D0D0D"
             ClipToBounds="True">

    <UserControl.Resources>
        <!-- Theme Colors -->
        <Color x:Key="BackgroundColor">#0D0D0D</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="TextColor">#E0E0E0</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>
        <Color x:Key="PanelColor">#181818</Color>
        <Color x:Key="SecondaryTextColor">#808080</Color>

        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}"/>

        <!-- Major Tick Brush -->
        <SolidColorBrush x:Key="MajorTickBrush" Color="#606060"/>
        <!-- Minor Tick Brush -->
        <SolidColorBrush x:Key="MinorTickBrush" Color="#404040"/>
        <!-- Beat Tick Brush -->
        <SolidColorBrush x:Key="BeatTickBrush" Color="#303030"/>
    </UserControl.Resources>

    <Border BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="0,0,0,1"
            Background="{StaticResource PanelBrush}">
        <Canvas x:Name="RulerCanvas"
                Background="Transparent"
                ClipToBounds="True"
                MouseLeftButtonDown="RulerCanvas_MouseLeftButtonDown"
                MouseMove="RulerCanvas_MouseMove"
                MouseLeftButtonUp="RulerCanvas_MouseLeftButtonUp"/>
    </Border>
</UserControl>
